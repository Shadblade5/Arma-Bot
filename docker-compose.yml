version: "3.9"
services:
  thebot:
    image: br1armabot:master
    restart: always
    environment:
      - BOT_DEBUG_ENVIRONMENT=0
  bot_db:
    image: mariadb
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: changethis
