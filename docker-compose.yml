version: "3.9"
services:
  thebot:
    image: deployed-br1bot:latest
    restart: always
  bot_db:
    image: mariadb
    restart: always
    volumes:
      - '/etc/br1bot/database/:/var/lib/mysql'
